// Simple cache clear function for data library page
export const clearAnalyticsCache = () => {
    // This function is called after CSV imports to clear any cached data
    // Currently, the data library doesn't have client-side caching, but this
    // function is kept for consistency and future use
    console.log("[Data Library] Cache cleared");
};
